# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.pytest_cache/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/

# Node / Frontend
node_modules/
frontend/dist/
frontend/build/
*.log

# Docker
*.tar

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Alembic (keep versions tracked, exclude local state)
alembic/versions/__pycache__/

# Vault (never commit actual tokens)
*.vault-token

# Exam PDFs (large binaries — tracked in MinIO, not Git)
Math_GS_Exams_English/

# Ingestion pipeline checkpoints (local dev only — can be large due to embeddings)
.ingestion_cache/
