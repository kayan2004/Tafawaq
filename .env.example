# Vault connection — the ONLY secret stored outside Vault in dev mode
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=dev-root-token

# Optional: override for local (non-Docker) development
# VAULT_ADDR=http://localhost:8200
